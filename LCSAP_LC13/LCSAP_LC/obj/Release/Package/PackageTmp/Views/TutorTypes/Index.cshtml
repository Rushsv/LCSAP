@model IEnumerable<LCSAP_LC.Models.TutorType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TutorType_description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TutorType_description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TutorType_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.TutorType_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TutorType_Id })
        </td>
    </tr>
}

</table>
